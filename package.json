{
  "name": "node_server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "deploy": "npm run build && gh-pages -d build/public",
    "fetch-feed": "node scripts/fetchFeed.js",
    "build": "npm run fetch-feed && gh-pages -d build"
  },
  "repository": {
    "type": "git",
    "url": "git+"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": ""
  },
  "homepage": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "request": "^2.88.2",
    "vercel": "^39.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
